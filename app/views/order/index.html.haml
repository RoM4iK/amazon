%h1 Cart
- if @order.order_items.empty?
    %h3 Cart is empty.
- else
    %h3= "You have #{pluralize(@order.order_items.count, 'items')} in your cart."
    = render partial: 'table'
    %div
        = link_to 'Checkout', checkout_index_path, { class: 'btn btn-primary pull-right' }
